/**
 * AdminPage - Página de Administración de Base de Conocimiento
 * 
 * Punto de entrada principal para gestionar la base de conocimiento
 * de Vademecum AI.
 */

import React from 'react';
import KBDashboard from '../components/admin/KBDashboard';

export default function AdminPage() {
  return <KBDashboard />;
}
