{
  "name": "farmacia-scraper-pro",
  "version": "1.0.0",
  "description": "Scraper masivo profesional para farmacias",
  "main": "index.js",
  "scripts": {
    "scrape": "npx ts-node --transpile-only scraper_local.ts",
    "process": "npx ts-node --transpile-only processor_ia.ts"
  },
  "dependencies": {
    "@google/generative-ai": "^0.2.1",
    "playwright": "^1.41.2"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3",
    "@types/node": "^20.11.19"
  }
}
